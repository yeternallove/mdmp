<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eternallove.mdmp">

    <!-- To auto-complete the email text field in the login form with mUseruser's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:screenOrientation="portrait">
        <activity android:name=".ui.activities.SplashActivity"
            android:theme="@style/AppThemeFullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeFullscreen" />
        <activity android:name=".ui.activities.MainActivity" />
        <activity android:name=".ui.activities.UserActivity" />
        <activity android:name=".ui.activities.DetailedActivity" />
        <activity android:name=".ui.activities.MeSettingActivity" />
        <activity android:name=".ui.activities.UserSettingActivity" />
        <activity android:name=".ui.activities.EditActivity" />
        <activity android:name=".ui.activities.ServerActivity" />
        <activity android:name=".ui.activities.ChangePwdActivity" />
        <activity android:name=".ui.activities.SettingActivity" />

        <meta-data
            android:name="com.eternallove.mdmp.util.GlideConfiguration"
            android:value="GlideModule" />
    </application>

</manifest>